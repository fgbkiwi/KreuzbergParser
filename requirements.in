# Direct dependencies (loose). Update with: ./scripts/update_deps.sh
# Resolved conflict-free pins are written to requirements.txt.

# Core OCR and Document Processing
kreuzberg
pytesseract
easyocr

# Deep Learning for Handwriting
# torch / torchvision come from the PyTorch CUDA index (default: cu130 / Blackwell)
transformers
pillow

# Interface Gráfica
flet

# Utilities
tqdm
psutil
python-dotenv

# PaddleOCR GPU: Kreuzberg native (AccelerationConfig cuda) + onnxruntime-gpu
# (ORT_DYLIB_PATH). Official paddleocr is the GPU fallback when Kreuzberg 4.10
# ignores ORT_DYLIB_PATH (bundled CPU ORT). Do not install paddlepaddle-gpu or rapidocr.
paddleocr>=3.7,<3.8
numpy>=1.24,<2.4
onnxruntime-gpu>=1.27
