# Core OCR and Document Processing
kreuzberg==4.2.12                   # Main document extraction library with Rust performance

# OCR Engines (Optional - Kreuzberg can use them)
pytesseract==0.3.13                 # Tesseract wrapper (CPU baseline)
easyocr==1.7.0                      # EasyOCR support (GPU via PyTorch)

# Deep Learning for Handwriting (Optional)
torch==2.6.0+cu124                  # PyTorch (detecta CUDA automaticamente)
torchvision==0.21.0+cu124           # Visão computacional
transformers==5.1.0                 # HuggingFace (TrOCR)
pillow==12.0.0                      # Image manipulation

# Interface Gráfica
flet==0.80.5                        # Modern UI framework

# Utilities
tqdm==4.67.3                        # Progress bars
psutil==7.2.2                       # System information
python-dotenv==1.2.1                # Configuration via .env
